# Sitebehaviour visitor analytics (landing page only). Copy to `.env` for local builds.
# Set the same variable in your host’s environment (e.g. Vercel/Netlify) for production.
PUBLIC_SITEBEHAVIOUR_SECRET=

# Optional: URL to a short form where visitors can suggest editors (Editors section + docs).
# PUBLIC_EDITOR_FEEDBACK_FORM_URL=
